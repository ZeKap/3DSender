#include <string.h>
#include <stdio.h>
#include <3ds.h>

/**
 * @brief Displays a line of text at the specified line and column.
*/
void print_line(size_t line, size_t column, const char * __restrict str, ...);
