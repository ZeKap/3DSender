#include <iostream>

#include "worker.hpp"

void Worker::connectToIp(const QString &ip, int port) {
    std::cout << "uwu i'm supposed to connect now" << std::endl;
    while (true) {
        
    }
}
